# Changelog

## Unreleased

### Changed

- 仕様1.12.0・ADR 0042で、段積み上段を荷室外へdragした時の作業位置をZ=0へ正規化し、空中に残らないよう変更した。積荷側面fitを20 mmから50 mmへ広げ、コンテナ内壁4面を候補へ追加し、既定ON・非永続・非履歴の `adjust` icon buttonで側面・内壁fitだけを切り替えられる。床・支持上面のZ snapは常時維持する。物理判定buttonは状態色と説明を維持して `information-box-outline` iconへ統一し、Schema `0.1.0` と既存JSON互換を維持する。

- 仕様1.11.0・ADR 0041で、単一支持グループのdrag判定から移動前の根・子孫をまとめて除外し、短距離移動で旧上段を支持物として誤認する不具合を修正した。床または上面の同じ高さにある隣接積荷へ20 mm以内で側面fitし、配置済み積荷を3D上で選択した後は現在視点に沿う矢印キーでX/Yを1 mmずつ調整できる。境界・支持喪失は修正途中配置として許容し、固定積荷との立体重複だけを停止する。押下・長押しはkey releaseで一回のUndo/Redoへまとめ、Schema `0.1.0` と既存JSON互換を維持する。

- 仕様1.10.2・ADR 0040で、コンテナ0件の3D viewport中央へ登録案内と既存コンテナ追加dialogを開くbuttonを追加した。コンテナ0件ではテンプレートインポートだけを理由付きで無効にし、テンプレートダウンロード、JSON読込、積荷だけの既存JSON互換、Schema `0.1.0`は維持する。

- 仕様1.10.1で、荷室外積荷の寄せbuttonをMaterial Design Iconsの `arrow-collapse-all` 相当iconへ変更し、寄せ実行時にcamera位置・注視点・視点方向・縮尺を変更しないよう改めた。寄せ位置、Project、保存、Undo/Redo履歴、Schema `0.1.0`は変更しない。

- 仕様1.10.0・ADR 0039で、全未配置積荷の向きを維持して選択中コンテナ近傍へ再整列し、全体へcameraを合わせる非保存・非履歴の磁石buttonをUndo左へ追加した。積荷selectorは重量表示と右側の青・黄・緑・灰の選択状態dotへ変更し、検索結果件数を撤去した。重心凡例へ選択中コンテナの総重量／耐荷重と全CLPの積込済数／全積荷数を追加した。viewerのwheel zoomを復活して `＋` / `－` buttonを撤去し、Ctrl中は十字矢印相当cursorと積荷上からのcamera panを優先する。Schema `0.1.0`、CSV、JSON、端末保存、既存Projectの意味は変更しない。

- 仕様1.9.0・ADR 0038で、アカウント・課金・クラウド保存を伴わないGitHub Pages技術試用版を承認した。Viteのbuild時base pathと、`main`へのpushまたは手動実行で固定SHAの公式Actionsを使って配信するworkflowを追加した。現行の静的・ローカルファースト境界とSchema `0.1.0`を維持し、Firebase Hosting・Authentication・backendは利用者、決済、権限、費用上限を確定した後の再評価候補として記録する。

- 仕様1.8.1でNavigation Drawerを再構成し、CLP、積荷一括登録、コンテナ、保存、読込、ヘルプの目的別表示へ整理した。短いbutton名、指定の横・縦配置、全幅close button、保存欄内の直近結果と端末保存注意を採用し、可視タイトル、積荷件数、自動保存説明、旧 `このブラウザ内`・`JSONファイル` 区分を撤去した。保存・読込・削除・確認・履歴・Schema `0.1.0` の動作は変更しない。

- 仕様1.8.0・ADR 0037でCSV 5列を維持したまま、Drawerから全積荷の「天地無用」と「上乗せ禁止」だけを一覧編集し、一回のUndo/Redoでまとめて往復できる機能を追加した。個別積荷editorと支持不適合理由も「上乗せ禁止」へ統一し、ONを既存 `canSupportCargo=false` へ反転する。制約変更で配置は自動移動・解除せず再判定し、横倒し配置を天地無用ONにする一括変更は原子的に拒否する。Schema `0.1.0` とCSV形式は変更しない。型・lint・単体32ファイル1,016件・ブラウザ104件・build・データ契約・ガバナンス・プロジェクト検査に合格し、進捗96%は据え置く。

- 仕様1.7.0・ADR 0035で、支持可の単一積荷に完全支持された上段を再帰的な移動グループとして扱い、座標dialogと3D dragで同じ差分だけ連動させる。移動は一回の履歴としてUndo/Redoし、子孫の座標失敗では全体をrollbackする。回転、複数支持、張り出し、支持不可接触は連動せず、子孫がある下段の荷室外移動・配置解除は拒否する。支持不可面との正面積接触には `support-permission-denied` と関連積荷名を使う専用説明を表示する。型・lint・単体1,014件・ブラウザ103件・buildに合格し、Schema `0.1.0`は変更しない。
- ADR 0036でCSV一括登録の確認文を、新規件数、既存積荷・配置の破棄、CLP名・隙間・コンテナの保持、端末保存の非自動更新を明示する文へ変更した。利用者は20件の一括登録と一回のUndoによる復元を確認済みだが、Windows版Excel往復は未実施である。
- 仕様1.6.0・ADR 0034のExcel向け固定CSVテンプレートと積荷一括置換を実装した。手動追加とCSV作成は一つの定数による30件上限、段積みOK・天地無用OFFを共通既定とし、Schema `0.1.0` の積荷・配置上限1,000件と既存31〜1,000件の読込・編集・削除・書出し互換は維持する。CSVは固定見出し、厳格なUTF-8・値検証、決定的ID、件数付き確認を使い、確定時だけ積荷を置換して配置を全削除し、一回のUndo/Redo対象とする。scene一時状態resetを適用・Undo・Redoで回帰し、型・lint・単体32ファイル1,006件・ブラウザ102件・buildに合格、指摘修正後の独立レビューにも合格した。Windows版Excel往復を残し、進捗は96%とした。
- 仕様1.5.1・ADR 0033に従い、選択中コンテナ内の保存済み積荷だけをBigInt・有理数で重量付き集計し、コンテナ幾何中心を赤、現在の積荷合成重心を黄の操作不可ドットとして3D内に表示する機能を実装した。両ドットと凡例swatchを同径10 CSS px・白い外枠なしとし、画面投影中心一致では位置をずらさず黄色が赤を完全に覆い、近接でも各投影中心を保って重複部分を黄色が覆う。画面外はclampやcamera自動変更をしない非数値statusとし、コンテナなし・空・利用可能・計算不能を区別して古い派生表示を残さない。DPR、camera、drag、履歴、読込成功・失敗、破損参照の読取専用回復表示、狭幅を全単体29ファイル975件・全ブラウザ97件で回帰し、型・lint・build・文書検査と独立コードレビューに合格した。使用上の重要事項は内容版1.2.0である。数値偏差、許容範囲、合否、安全保証、コンテナ自重、drag preview追従、永続化は対象外で、Schema `0.1.0` は変更しない。人間による差分視認性確認を残すため重量バランス可視化は4/5、現在進捗は97%とする。
- 現在値・現在地のGit報告に、ローカル状態と実際に照会したリモート状態、未push・未取り込み件数、確認日時を含める。接続失敗や未確認の状態を同期済みと扱わない。
- 共通ガバナンス3.0.0、文書移行契約1.0.1、task交代契約2.0.0を採用する。ガバナンス変更による強制交代・旧ownerとの起動handshake・交代専用記録への依存を廃止し、ユーザー依頼時の通常repository再開へ統一する。検証は7分類と明示runtimeに対応し、既存総合9ゲートを維持する。仕様1.4.2、Schema 0.1.0、製品動作、依存、受入残条件、進捗98%は変更しない。

- 共通ガバナンス1.5.0の影響ベース検証へ移行し、6つの変更class、iteration・targeted regression・completion・release-only stage、gate包含、evidence失効、省略理由、unknown impact時のcomprehensive fallbackをproject-owned policyと運用matrixで管理する。製品仕様1.4.2、Schema `0.1.0`、製品動作、依存関係、進捗98%は変更しない。
- 仕様1.4.2・ADR 0025 refinementで通常デスクトップ高のApplication Shellをbrowser viewport高へ合わせ、上下padding、Application Barと余白、コンテナtabを除いた残りを3D viewportへ配分した。通常時の一般案内帯を撤去し、drag・失敗・操作不可statusだけをcanvas寸法・位置を変えないviewport内浮動表示として維持する。Schema `0.1.0`と進捗98%は変更しない。
- 仕様1.4.1で主ページの入力データ注意を内容版1.1.0の「使用上の重要事項」へ集約し、Drawer最下部にpackageのAuto CLPアプリ版とCLPデータ形式版を追加した。法的な利用規約は将来checkpointのまま、Schema `0.1.0`と進捗98%は変更しない。
- 仕様1.4.0・ADR 0030でPhase 1をコンテナ専用とし、利用者向け登録対象名を「コンテナ」へ統一した。積荷cardとコンテナcardを通常画面から撤去し、3Dで選択中のコンテナの追加・編集・非cascade削除をNavigation Drawerから開くmodalへ移した。単体28ファイル952件、現行ブラウザ84件、型、lint、build、文書・データ・ガバナンス検査で回帰し、内部 `Container` / `containerId`、Schema `0.1.0`、既存保存データ、進捗98%は変更しない。
- 同じ人間のプロジェクト評価者が仕様1.0.1のWebGL 2非対応画面と初期描画失敗画面を再確認し、操作停止、現在の作業データと端末保存済みデータの違い、退避入口、復旧手順を理解できると判定した。作業中のcontext loss遷移は対象ブラウザ回帰6件の合格で確認し、人間試用では安全な障害注入ができず未再現であることを明記した。正式fixture、評価者区分、実務利用者試用は未完了で、Schema 0.1.0と進捗98%は変更しない。
- 同じ人間のプロジェクト評価者による仕様1.3.0の差分試用合格を、受入集約・人間観察証拠・ロードマップへ同期した。Drawer背景close、Drawerだけの積荷・候補追加入口、現行UIからの自動配置提案除外を受入可能とし、途中の意図しないreload/reloadは最終判定へ影響しなかった。正式fixture、仕様1.0.1のWebGL阻止・救出画面、実務利用者試用は未完了で、Schema 0.1.0と進捗98%は変更しない。
- 仕様1.3.0・ADR 0029で、積荷追加と候補追加の入口をNavigation Drawerへ集約し、Drawer背景のクリックで背面操作を発火させず閉じるようにした。自動配置提案は実装時期が後になるためPhase 1の通常ページと通常起動経路から外し、domain・Worker・session/view・panel・tests・AP-08証拠は将来技術資産として保持する。単体28ファイル952件と現行browser83件を回帰し、将来自動提案browser 10件は再公開時に再接続する非実行snapshotとして通常suiteから明示分離した。Schema 0.1.0と進捗98%は変更しない。
- 仕様1.2.2・ADR 0028で、Navigation Drawerから開く独立した `操作方法` dialogを追加し、3Dの積荷選択、視点回転・平行移動、移動・回転、履歴、判定、下段操作を案内する。寸法矢印を `4 × 4` へ縮小し、現在候補の配置copyを `現在の座標` へ改める。`使用上の重要事項`、CLP、履歴、Schema 0.1.0、進捗98%は変更しない。
- 仕様1.2.1・ADR 0027で、荷室tabを3D viewportの欄外上部へ移し、選択積荷の可視寸法を`整数 mm`と両端外向き矢印へ簡略化し、物理判定lampを状態別icon-only表示へ変更した。状態名・件数・理由・非保証説明はaccessible name、title、dialogへ維持する。camera平行移動、Schema 0.1.0、進捗98%は変更しない。
- 仕様1.2.0・ADR 0026の右端menu、一行scroll tab、side-relative荷室外anchor、候補間共有camera、選択積荷の3軸寸法annotation、selector直下の固定context action row、物理判定lamp/dialog、版付き「使用上の重要事項」を実装した。単独支持共通の積荷別 `structure-stability-unverified` はdomain・Worker・自動提案から廃止し、複数支持・隙間・張り出し等の `support-conditions-unverified` は維持する。`automatic-proposal-v2` のAP-08証拠を再記録し、Schema 0.1.0と進捗98%は不変。
- 仕様1.1.1で、積荷選択だけの成功通知とviewport直下の重複する一般的非保証文を除き、狭幅時の候補selectorを内容高へ修正し、荷室全体表示をaccessibleな `cube-outline` 相当icon-only buttonへ変更した。利用規約整備を公開・実務運用前の将来工程として記録し、具体的な物理未確認表示、Schema 0.1.0、進捗98%は変更しない。
- 3D viewportを主作業面とし、Application Bar、3D能力Chip、CLP操作をまとめたNavigation Drawer、CLP設定dialog、viewport内の候補・積荷検索selectorへ再構成した。未保存確認付き新規CLPは新しいidentityと空履歴を作るbarrierとする。仕様1.1.0、ADR 0025。Schema 0.1.0は不変。
- 利用者向けの「案件」を「CLP」、自動提案の未適用結果を「配置案」へ統一した。WebGL障害時の「作業データ」、内部 `Project` / `projectId`、Schema `0.1.0`、固定JSONファイル名は変更しない。仕様1.0.2、ADR 0024。

### Added

- 複数候補のside-relative作業面とcamera選択肢、および積荷画像のthumbnail・selected-only sprite・永続化選択肢を比較する設計文書を追加した。複数候補部分は後にADR 0026でside-relative anchorと共有cameraへ採用・修正し、積荷画像部分は未承認のまま維持する。
- Undo/Redo・拡大縮小と同じ固定toolbarへ常設するX/Z回転button、一本の軸線へ矢印が回り込む同一glyphの90度差、未選択・天地無用・busy時の理由付き無効状態、およびADR 0021。
- drag中だけ操作対象以外の積荷をほぼ透明な中立面・灰色点線とし、支持候補を緑または黄の点線で示して終了時に通常表示へ戻す集中表示とADR 0020。
- 床または支持可能な積荷上面へ3D drag中のZをsnapし、単一支持面内のX/Y制限、条件未確認preview、候補強調、一回のUndo/Redoを提供する支持面操作とADR 0019。
- 共通ガバナンス1.4.0の容量経路に対応するプロジェクト調整runbook、exact-task session測定script、handoff index/record、ADR 0016。
- 共通ガバナンス1.3.0とプロジェクト固有ルール。
- 現行仕様、運用手順、ADR、重み付きロードマップ。
- 開発、テスト、コード調査、公式文書調査、レビュー、UIテストの役割設定。
- ローカルWebアプリ、3D直方体モデル、物理制約に関する初期判断。
- 正規単位・値域、矩形開口、積荷別許可回転、Phase 1の支持・荷重に関するADR 0005〜0008。
- コンテナ局所右手座標、負X側開口面の原点、向き適用後AABB最小角の配置アンカーを定めるADR 0010。
- viewport上のwheelをpage scrollへ戻し、明示buttonだけでcamera zoomし、配置済みdragのX/Y正面積overlap境界と共通 `大きさ` copyを定めるADR 0015。
- 荷室外の自由な作業位置、X/Z軸回転、天地無用入力補助、compactな履歴・選択UI、cameraとpage位置の保持を定めるADR 0017。
- 軸別固定隙間を隣接表面間の実距離とし、配置後の適用面、床・支持例外、非支持ペアの分離軸規則を定めるADR 0011。
- 境界違反から支持不足・隙間不足を連鎖させず、独立した不適合・未確認理由を保持するADR 0012。
- 選択候補の内部線枠、負X側中央開口、登録済み配置を描画するProject→scene adapterと3D候補選択。
- 負座標・外側配置を含む全投影範囲へ適応するcameraと、奇数mm中心・極値・候補切替を覆う単体・ブラウザ回帰。
- 保存前draftを案件から分離した配置追加、整数mm座標編集、積荷ごとの許可向き変更、明示的な配置取り外しフォーム。
- WebGL非対応時の配置操作、編集中の候補lock、stale対象の安全な終了とフォーカス復帰、305〜375px表示を覆う回帰。
- 3D上の積荷選択、床面方向の取引的drag、回転・平行移動・拡大縮小・視点resetと、選択状態の配置一覧同期。
- 3D内の明示的な `＋` / `－`、遠方積荷に左右されない「荷室全体を表示」と、全投影範囲へのcamera到達余地を保つ距離境界。
- 配置済み積荷のX/Y footprintと荷室床面の正面積overlapを判定する純粋geometry helper、および完全drag-outを仮置き復帰する一回の削除履歴として検証する単体・browser回帰。
- 選択積荷のmesh近傍へ追従し、許可済みの床面90度相手向きだけを最小角保持・Undo可能な1操作で適用するアクセシブルな回転UI。
- 案件全体で未配置の積荷を荷室外へ決定的に派生表示し、荷室内dropだけを一回の配置追加として確定する非永続仮置き場。
- 3D直前へ移した単一の案件履歴操作と、選択積荷の結果指向寸法・位置、既存座標フォームへの導線をまとめるscene近傍操作カード。
- scene差分を正規開始座標へ最近接1 mmで適用する純粋adapter、drag取消・描画障害時rollback、タッチ選択と縦スクロールfallbackを覆う回帰。
- コンテナ内部への包含と、面・辺・角接触を除く正体積AABB重なりを判定する純粋geometry基盤。
- 配置後の壁・天井・床例外を含む隙間境界と、非支持ペアの共有表面間距離を判定する純粋geometry基盤。
- 矩形開口の2Y・1Z隙間式と、許可向きから寸法上通る向きを抽出する純粋geometry基盤。
- 非負safe integerの積荷質量をoverflowなく合計し、耐荷重の等値・超過を区別する純粋な評価基盤。
- 一つ以上の支持上面のXY矩形和集合が積荷底面を100%覆うか、1 mmの欠け・重複・外側クリップを含めて判定する純粋geometry基盤。
- 床支持と、段積み許可された積荷上面の完全一致Z接触・XY 100%被覆を合成する純粋な幾何支持判定。
- 対象コンテナの配置を抽出し、境界・重なり・隙間・開口・支持・耐荷重の独立理由、集約状態、計算不能を返す純粋な物理判定。
- 物理判定をメインスレッド外で実行し、状態と件数を先に、全理由を25件ずつ返すローカルmodule Worker。
- 適合・不適合・未確認・判定不能、対象・関連積荷、独立理由をWebGL非対応時も表示するアクセシブルな物理判定パネル。
- 案件JSON Schema `0.1.0`、意味契約、純粋なdomainモジュール境界、依存不要のデータ契約チェック。
- TypeScript、React、Three.js、ViteによるローカルWebアプリ骨格、WebGL 2能力確認画面、能力確認用3D描画。
- 6種類の向き適用を行う純粋なdomain関数と、型検査、lint、単体、ブラウザ、ビルドの独立検証。
- 完全なreadonly案件型、Ajv Draft 2020-12による構造検証、ID・参照・許可向き・開口関係・安全整数合計の意味検証。
- 5 MiB上限、安定した非反射code/path、検証済みJSON書出し、派生計算成功後だけ状態を置換する取引的読込基盤。
- 案件名・軸別隙間・積荷・コンテナ候補を、入力途中のdraftと確定済み案件を分離して追加・編集・一覧・明示削除できるローカル入力UI。
- 正確なmm・kg入力境界、積荷別許可向き、参照を壊さない不変コマンド、キーボード・フォーカス・単位説明、305〜375pxの狭幅表示に対する検証。
- 案件設定・積荷・候補・配置・1回の3D床面dragを最大100件取り消し・やり直しできる、Project参照を構造共有する非永続履歴とアクセシブルな操作UI。
- stale/no-op/分岐、未保存入力中のlock、native入力履歴と独自入力コンポーネントの保護、WebGL非依存、305〜375px表示を覆う履歴回帰。
- IndexedDB `current-project` 単一手動枠のtransaction完了後保存・読込・確認削除と、固定名 `auto-clp-project-0.1.0.json` の標準file input/download。
- 端末保存・JSON入出力をまとめるモーダルNavigation Drawerと、処理中・成功・失敗を操作ごとに識別できるアクセシブルなSnackbar。
- 全候補を置換前に再判定するone-shot module Worker、履歴barrier、非同期interaction generation、固定code失敗表示、削除確認focus管理。
- 実IndexedDB reload/delete、download/reimport、全JSON失敗段階、blocked/abort/破損、遅延競合、WebGL非依存、狭幅、1,000配置・100候補Worker応答性を覆う回帰。
- 手動の端末内保存、保持・削除、JSONファイル、競合・rollback境界を定めるADR 0013。
- 4本の匿名合成Phase 1受入ケース、共通合格基準、canvas追加判断、観察記録様式。
- 床突き抜けを他の境界外と区別し、独立理由を保持するADR 0014。
- 全積荷を一候補へ配置する目的関数、DFS列挙、決定的attempt上限と境界、cutoff、preview、一括適用を定めるAccepted ADR 0004と数値入り合成受入AP-01〜08。
- 検証済みProject用の純粋な自動提案DFS、目的順位、向き・候補点列挙、決定的attempt予算、cutoff集約、未確認理由保持と31件の専用単体試験。
- 正本Schema・意味検証付きone-shot自動提案Worker、厳格な応答guard、固定code client、即時terminate取消・遅延応答maskと80件の専用単体試験。
- Project参照・interaction generationのstale判定、取消・retry race、source ID相関、固定安全copy、25件pageを持つReact非依存の自動提案session/viewと64件の専用単体試験。
- WebGL非対応時も使える自動提案React panel、AppのProject/interaction generationとbusy gate、実Worker開始・取消・再試行、未適用preview、候補・配置・未確認事項の25件page。
- 実Workerの空・完全案、Project・履歴の非変更、編集・Undo/Redo・保存・JSON置換によるstale、controlled Worker終了250 ms、遅延結果破棄、ARIA、305〜375pxを覆う7件のブラウザ回帰。
- 自動提案をSchema・意味・正本物理判定で再検証し、配置だけを深いcopyで一括置換する純粋な適用境界、identityを一度だけ取得する確認、同一案no-op、適用済み・変更なし表示。
- `自動提案の一括適用` を一件だけ記録するUndo/Redoと、AP-02/AP-03、改ざん拒否、警告保持、stale・busy・二重適用、狭幅・WebGL非依存を覆う36件の追加単体・4件の追加ブラウザ回帰。
- production module Workerを使うAP-08匿名20積荷性能回帰と、cold/warm、決定的result hash、main timer/rAF、native取消、console、実行環境を保存する技術証拠。
- 匿名合成AC-01〜04のCodex UI-assisted部分観察記録。exact floor dragの反復修正、通常JSON再読込、WebGL非対応時のZ編集・判定・履歴・端末保存、狭幅、主要focus、consoleを確認し、未実行操作・環境遅延・改善候補・残る人間試用を区別。
- 固定loopback URLとstrict portを使うUI試用server script、および固定済みPlaywright Chromiumを導入する初期設定script。
- 人間のプロジェクト評価者がChromeで匿名派生ケースの移動・向き・削除・履歴、1 mm支持不足、床突き抜け、端末保存、JSON往復を完了し、床突き抜け由来の支持不足カスケード不具合を発見した案内付き人間評価証跡。

### Changed

- 仕様を1.0.1へ更新し、WebGL障害時の「JSON救出」を、人が対象・含む内容・固定出力名・ダウンロード開始結果・次行動・復旧手順を理解できる「作業データのファイル退避」画面へ修正した。現在の作業データと端末保存済みデータを区別し、Schema 0.1.0と進捗98%は変更しない。
- 仕様を1.0.0へ更新し、WebGL 2能力確認と初回3D描画成功をAuto CLPの必須動作条件とした。非対応・初期描画失敗・context lossでは案件編集、配置、判定、自動提案、履歴、保存操作を全面停止し、現在案件と端末保存の読み取り専用JSON救出だけを提供する。Schema 0.1.0と進捗98%は変更しない。
- 同じ人間のプロジェクト評価者による `HUT-01` A/B派生fixtureの再試験で、床貫通1件だけの表示、Z=0修正後の単独支持と構造・安定性未確認、Undo/Redo往復が期待どおりと確認された。診断上の床Z=0正規化は描画・保存座標を動かさず、各配置は独立した絶対座標で自動追従しない。狭幅・Tab・focus・WebGL fallback・正式fixture・評価者区分は未完了で、進捗98%は変更しない。
- 同じ人間のプロジェクト評価者による仕様0.18.1の案内付き再試験で、天地無用とX/Z回転可否、固定toolbar、zoom同等の有効枠、端末再読込、F回転時のHを含む他積荷の位置維持が期待どおりと確認された。`HUT-01` 再試用、正式fixture、評価者区分、狭幅・Tab・WebGL fallbackは未完了で、進捗98%は変更しない。
- 仕様を0.18.1へ更新し、未配置積荷の初期gridを許可向き全体の最大X/Y footprintで固定した。一つの積荷を荷室外で回転しても、他の未配置積荷は再配置されない。Schema 0.1.0と進捗98%は変更しない。
- 仕様を0.18.0へ更新し、積荷editorの向き設定を天地無用だけへ簡略化した。旧許可向き部分集合は読込時に天地無用ONの2向きまたはOFFの全6向きへ正規化し、Z軸床面回転を常時可、X軸回転を天地無用だけで制限する。回転buttonの使用可は紫色の塗りから拡大・縮小と同じ青緑の強調枠へ変更した。Schema 0.1.0と進捗98%は変更しない。
- 仕様を0.17.1へ更新し、回転buttonの使用可を3Dの青緑線および積荷色と混同しない明るい紫色、使用不可を暗い低彩度の背景・枠・前景へ分け、opacity差だけに依存しない視認性へ修正した。Schema 0.1.0と進捗98%は変更しない。
- 仕様を0.17.0へ更新し、mesh追従回転controlとdistinct SVG pathを、連続回転で位置が動かない固定toolbarと同一glyphの90度差へ変更した。Schema 0.1.0と進捗98%は変更しない。
- 仕様を0.16.0へ更新し、矩形開口の寸法不適合だけを積荷別理由として表示し、完全な搬入経路と安全を保証しない範囲は恒常的な注意へ集約した。Schema 0.1.0と進捗98%は変更しない。
- 支持判定を、複数面の100%和集合から、単一上面のX/Y完全包含、複数支持・隙間・張り出し・支持可否混在の `support-conditions-unverified`、支持接触不成立の不適合へ変更。自動提案は床置きまたは単独支持だけを生成する。
- 仕様を0.14.0へ更新し、fine-pointer床面dragを未配置・配置済み共通の `xy-contained` / `partial` / `outside` に分類した。partialは修正途中の不適合配置として保存し、outsideだけを非永続作業位置または一回の配置削除とする。全Project積荷の検索select、compact選択cardのCRUD入口、積荷定義・配置別modal editor、非cascade削除、distinct X/Z icon、固定高statusを導入した。Schema 0.1.0と進捗98%は変更しない。
- 仕様を0.13.0へ更新し、未配置積荷の荷室外drop位置・向きをUI session内で保持し、積荷pickerからcamera移動なしに選択できる作業スペースへ拡張した。回転はicon-onlyのX/Z軸操作とし、天地無用で横倒し向きを制限する。荷室へ正面積で重なる外側回転は拒否し、積荷編集後の無効な外側poseは決定的gridへ戻す。Undo/Redoをzoomと同じviewport toolbarへ移し、Project更新時のcameraと履歴実行時のpage位置を保持する。選択cardは積荷名を見出しとする2列compact表示へ整理し、寸法prefixを除いた。Schema 0.1.0と進捗98%は変更しない。
- 複数候補をtab等で切り替えて荷室外の退避関係を維持するUIと、積荷画像・写真の表示は、承認済みだが未実装の将来要件として分離した。
- 仕様を0.12.0へ更新し、viewport上のwheelはpage scroll、camera zoomは `＋` / `－` buttonだけとした。配置済みdragは量子化後のno-opを維持し、X/Y正面積overlapがあれば部分overhangを保存して物理診断へ渡し、面・辺接触を含む面積0なら一回の `placement.delete` として仮置きへ戻す。選択カードと配置一覧の寸法labelは `大きさ` に統一した。Schema 0.1.0と進捗98%は変更しない。
- 管理済み共通ガバナンスを1.3.0から1.4.0へ同期。4つの容量aliasを現在task ID、個別300 MiB、別指標のCodex全体10 GiB、scan完全性、独立終了コードへ経路指定し、全アクティブAuto CLP taskの交代を要求。
- 仕様を0.11.0へ更新し、未配置積荷の非永続仮置きとfine-pointer初回配置、fallback、保存・物理判定との境界を確定。
- 検証済みの仮置き場とdrag初回配置に基づきAuto CLPロードマップ進捗を96%から97%へ更新。
- 検証済みのscene近傍履歴・座標導線と結果指向の向き表示に基づきAuto CLPロードマップ進捗を97%から98%へ更新し、3D表示と手動配置マイルストーンを完了。
- 仕様を0.2.0へ更新し、Phase 1の単位、開口部通過、回転、支持・荷重の規則と未確認境界を確定。
- 検証済み設計成果に基づきAuto CLPロードマップ進捗を4%から6%へ更新。
- 仕様を0.3.0へ更新し、案件JSONの保存範囲、上限、意味検証、トランザクション読込を確定。
- 検証済みデータ契約成果に基づきAuto CLPロードマップ進捗を6%から8%へ更新。
- 検証済みの最小アプリ骨格と技術スパイクに基づきAuto CLPロードマップ進捗を8%から9%へ更新。
- 検証済みの完全案件契約と取引的読込基盤に基づきAuto CLPロードマップ進捗を9%から10%へ更新し、基盤・データ契約マイルストーンを完了。
- 検証済みの案件・積荷・候補入力UIに基づきAuto CLPロードマップ進捗を10%から25%へ更新し、積荷・コンテナ入力モデルのマイルストーンを完了。
- 仕様を0.4.0へ更新し、未定義だった `positionMm` の原点、軸、最小角アンカー、整数mmとscene派生値の境界を確定。
- 承認済み座標契約に基づきAuto CLPロードマップ進捗を25%から26%へ更新し、3D表示・手動配置マイルストーンを開始。
- 検証済みの候補選択と案件3D投影に基づきAuto CLPロードマップ進捗を26%から31%へ更新。
- 検証済みのフォーム手動配置に基づきAuto CLPロードマップ進捗を31%から37%へ更新。
- 検証済みのcanvas直接選択・床面drag・視点操作に基づきAuto CLPロードマップ進捗を37%から45%へ更新。
- 検証済みの境界・重なりgeometry基盤に基づきAuto CLPロードマップ進捗を45%から47%へ更新し、物理制約マイルストーンを開始。
- 仕様を0.5.0へ更新し、承認済みの軸別隙間意味を確定。独立した設計ゲートとしてAuto CLPロードマップ進捗を47%から48%へ更新。
- 検証済みの隙間境界・非支持ペアgeometry基盤に基づきAuto CLPロードマップ進捗を48%から50%へ更新。
- 検証済みの矩形開口寸法・許可向きgeometry基盤に基づきAuto CLPロードマップ進捗を50%から52%へ更新。
- 検証済みの総質量・耐荷重評価基盤に基づきAuto CLPロードマップ進捗を52%から53%へ更新。
- 検証済みの支持面矩形和集合被覆基盤に基づきAuto CLPロードマップ進捗を53%から54%へ更新。
- 検証済みの床・Z接触・段積み可・100%被覆の支持合成基盤に基づきAuto CLPロードマップ進捗を54%から55%へ更新。
- 仕様を0.6.0へ更新し、境界不適合を優先しながら幾何支持接触と独立理由を保持する物理診断規則を確定。
- 検証済みの高位物理判定と独立診断に基づきAuto CLPロードマップ進捗を55%から59%へ更新。
- 検証済みの理由表示UI、ブラウザ統合、1,000配置の実Worker性能ゲートに基づき物理制約マイルストーンを完了し、Auto CLPロードマップ進捗を59%から65%へ更新。
- 仕様を0.7.0へ更新し、案件操作履歴の対象、上限、除外状態、ショートカット、非永続境界を確定。
- 検証済みの案件undo/redoに基づきAuto CLPロードマップ進捗を65%から67%へ更新。
- 仕様を0.8.0へ更新し、単一手動端末保存、JSONファイル入出力、全候補事前判定、履歴barrier、保持・削除・競合規則を確定。
- 検証済みの端末保存・JSON入出力に基づき保存・再読込・操作性マイルストーンを完了し、Auto CLPロードマップ進捗を67%から77%へ更新。
- 仕様を0.9.0へ更新し、床突き抜け専用診断と匿名合成受入契約を確定。受入設計証拠に基づきAuto CLPロードマップ進捗を77%から78%へ更新。
- 床突き抜け・支持1 mm境界・通常フォーム・履歴・保存・JSON・WebGL fallbackの自動証拠に基づきAuto CLPロードマップ進捗を78%から79%へ更新。
- 仕様を0.10.0へ更新し、自動提案の目的関数、探索上限、最適性非保証、打切り表示、適用境界を確定。設計ゲート完了に基づき進捗を79%から81%へ更新。
- 純粋な自動提案domain探索と境界試験の検証に基づき、Auto CLPロードマップ進捗を81%から85%へ更新。Worker、preview、適用は未実装。
- 未接続の自動提案Worker transportと取消・応答maskの検証に基づき、Auto CLPロードマップ進捗を85%から87%へ更新。App接続、preview、適用は未実装。
- 未接続の自動提案session/viewの検証に基づき、Auto CLPロードマップ進捗を87%から88%へ更新。React/App接続、preview DOM、適用は未実装。
- 自動提案のReact/App接続と実Worker previewの検証に基づき、Auto CLPロードマップ進捗を88%から91%へ更新。確認付き適用、Undo、AP-08代表規模の実時間性能は未実装または未検証。
- 自動提案の確認付き一括適用と一回のUndo/Redoの検証に基づき、Auto CLPロードマップ進捗を91%から93%へ更新。AP-08代表規模の実時間性能は未検証。
- 記録環境でのAP-08実Worker性能・決定性・取消の検証に基づき、自動提案マイルストーンを完了し、Auto CLPロードマップ進捗を93%から94%へ更新。一般端末SLA、最低GPU、実務利用者受入は未検証。
- 案内付き人間評価の独立証跡に基づき、Auto CLPロードマップ進捗を94%から95%へ更新。床突き抜け由来の支持不足カスケード修正を最優先とし、明示zoom・荷室基準の視点復元、非永続仮置き場、選択積荷近傍の許可向き回転、scene近傍の座標・履歴、時間的に識別できる通知、保存Navigation Drawerを後続UI改善、JSON名変更を仕様・ADR承認待ちとして分離。
- 既知の物理診断仕様違反 `HUT-01` を反映し、物理制約マイルストーンを20/20 Completeから19/20 In progressへ訂正して、ロードマップ進捗を95%から94%へ更新。修正と回帰完了まで1点を保留。
- `HUT-01` の診断修正と全回帰・独立レビュー合格に基づき、物理制約マイルストーンを20/20 Completeへ戻し、ロードマップ進捗を94%から95%へ更新。人間による同じfixtureの再試用は未実施。

### Fixed

- 床下配置を一般的なコンテナ境界外と同じ理由でしか説明できなかった診断契約を、専用の床突き抜け理由へ分離。
- 床をZ=0へ直せば完全支持になる上段へ、床突き抜けだけから `support-not-full` とpair隙間理由を連鎖表示していた物理診断。実支持未成立時は構造・安定性未確認を増やさず、無関係な支持不足と独立理由は保持。
- プロジェクト文書検証がPlaywrightの一時 `test-results` 内Markdownを正本文書として数えないよう修正。
- 並行ブラウザ試験が固定portの別runnerへ誤接続し、所有していないserverの停止後に連鎖失敗できた実行基盤を、runner所有の動的loopback serverと固有Playwright output directoryへ分離。成功時だけ一時成果物を削除し、失敗時は診断pathを保持する。

### Removed

- Phase 1の通常ページに表示していた自動配置提案panel・開始・適用入口、およびメインページ上の積荷追加・候補追加入口。将来自動配置の技術資産と各追加機能自体は削除しない。
- WebGL 2非対応時にフォーム、物理判定、履歴、保存・再読込、自動提案を利用できた通常操作fallback。
- 寸法上矩形開口に収まる積荷ごとに生成していた `opening-path-unverified` 理由と重複メッセージ。

### Security

- 3D表示を確認できない状態で空間配置や判定を変更できないよう、能力確認中と描画障害時の操作ゲートを全面化。データ救出は固定名downloadのread-only経路に限定した。
- 初期版を端末内完結とし、ネットワーク送信と外部書き込みを既定で禁止。
- JSONを解析する前の5 MiB上限、追加項目拒否、意味検証、失敗時の既存状態保持を設計契約へ追加。
- 開発サーバーをループバックに限定し、外部通信を行わないローカル描画と、非対応・初期化失敗・描画例外・WebGLコンテキスト喪失を明示する能力ゲートを追加。
- 案件JSONの申告サイズと実UTF-8サイズを解析前に確認し、入力値・未知キーをエラーへ反射せず、無効な案件を書き出さない境界を追加。
